package com.example.face_recognition_attendance_projects.configureOwnId;

import org.springframework.stereotype.Service;

@Service
public class OwnIdCreation {
	/*
	 * private EntityManager entityManager;
	 * 
	 * private static final String ID_PREFIX = "TRI";
	 * 
	 * @Transcation public synchronized String generatedNextId() {
	 * 
	 * String lastId = (String)
	 * entityManager.createQuery("SELECT MAX(e.id) FROM Emplyoee e").getSingleResult
	 * ();
	 * 
	 * int nextNumber = 100; if (lastId != null) { int lastNumber =
	 * Integer.parseInt(lastId.replace(ID_PREFIX, "")); nextNumber = lastNumber + 1;
	 * } return ID_PREFIX + nextNumber; }
	 */
}
